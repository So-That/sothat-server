spring.application.name=Kafka_ES
spring.kafka.bootstrap-servers=localhost:9092
spring.data.mongodb.uri=mongodb://${MONGODB_USERNAME}:${MONGODB_PASSWORD}@${MONGODB_HOST}/${MONGODB_DB}?authSource=admin
gpt.servers=localhost:8000
cors.allowed-origin=localhost
youtube.api.key=


